<nav class="navbar navbar-expand-md navbar-light bg-light fixed-top">
  <%= link_to "BeeCycle", root_path, class: "navbar-brand" %>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <% if user_signed_in? %>
          <li class="nav-item"><%= link_to "Annonces", listings_path, class: "nav-link" %></li>
          <li class="nav-item"><%= link_to "Boîte de réception", conversations_path, class: "nav-link" %></li>
          <li class="nav-item"><%= link_to "Locations", rentals_current_path, class: "nav-link" %></li>
        <% else %>
          <%= link_to "Inscription", new_user_registration_path, class: "dropdown-item" %>
          <%= link_to "Connexion", new_user_session_path, class: "dropdown-item" %>
        <% end %>
    </ul>

    <% if user_signed_in? %>
      <ul class="navbar-nav ml-auto">
        <li class="nav-item dropdown">
          <a class="nav-link" href="#" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
            <%= image_tag current_user.avatar.url(:mini), class: "rounded-circle" %>
          </a>
          <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdownMenuLink">
            <%= link_to "Modifier le profil", edit_user_registration_path, class: "dropdown-item profile" %>
            <%= link_to "Déconnection", destroy_user_session_path, method: :delete, class: "dropdown-item" %>
          </div>
        </li>
      </ul>
    <% end %>

    <!--<%= form_tag search_path, method: "get", class: "form-inline my-2 my-lg-0" do %>
      <%= text_field_tag "city", nil, {class: "form-control mr-sm-2", placeholder: "Partout"} %>
      <button class="btn btn-outline-success my-2 my-sm-0" type="submit"><i class="fa fa-search" aria-hidden="true"></i></button>
    <% end %>-->
  </div>
</nav>
