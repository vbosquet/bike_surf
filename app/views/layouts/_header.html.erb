<header class="navbar navbar-fixed-top navbar-inverse">
  <div class="container">
    <%= link_to "Bike Surf", root_path, id: "logo", class: "navbar-brand" %>
    <nav>
      <ul class="nav navbar-nav navbar-right">
        <% if user_signed_in? %>
          <li class="dropdown">
            <a class="dropdown-toggle" href="#" data-toggle="dropdown">Hôte <span class="caret"></span></a>
            <ul class="dropdown-menu">
              <li><%= link_to "Gérer mes annonces", listings_path %></li>
            </ul>
          </li>
          <li class="nav-item"><%= link_to "Voyages", "#" %></li>
          <li class="dropdown">
            <a class="dropdown-toggle" href="#" data-toggle="dropdown">Paramètres <span class="caret"></span></a>
            <ul class="dropdown-menu">
              <li><%= link_to "Modifier le profil", edit_user_registration_path %></li>
              <li class="divider"></li>
              <li><%= link_to "Déconnection", destroy_user_session_path, method: :delete %></li>
            </ul>
          </li>
        <% else %>
          <li><a href="#" data-toggle="modal" data-target="#signup">Inscription</a></li>
          <li><a href="#" data-toggle="modal" data-target="#login">Connection</a></li>
        <% end %>
      </ul>
    </nav>
  </div>
</header>